<odoo>
    <record id="vpicktree" model="ir.ui.view">
        <field name="name">stock.picking.tree</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree"/>
        <field name="arch" type="xml">
            <field name="scheduled_date" position="after">
                <field name="date" readonly="1"/>
            </field>
        </field>
    </record>

    <record id="view_picking_form" model="ir.ui.view">
        <field name="name">stock.picking.form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <field name="date_deadline" position="after">
                <field name="date" attrs="{'readonly': [('state', 'in', ('done', 'cancel')),]}"/>
            </field>
        </field>
    </record>

    <record id="view_picking_internal_search" model="ir.ui.view">
        <field name="name">stock.picking.internal.search</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search"/>
        <field name="arch" type="xml">
            <filter name="expected_date" position="after">
                <filter string="Accounting Date" name="date" domain="[]" context="{'group_by': 'date'}"/>
            </filter>
            <filter name="expected_date" position="after">
                <filter string="Done Date" name="date_done" domain="[]" context="{'group_by': 'date_done'}"/>
            </filter>

        </field>
    </record>
</odoo>