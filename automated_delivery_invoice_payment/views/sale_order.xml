<odoo>
    <record id="view_stock_picking_form" model="ir.ui.view">
        <field name="name">view_stock_picking_form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="priority" eval="1000"/>
        <field name="arch" type="xml">
            <button name="button_validate" position="after">
                <button name="button_validate_with_backorder" attrs="{'invisible': ['|', ('state', 'not in', ('waiting', 'confirmed')), ('show_validate', '=', False)]}" string="Validate with Backorder" type="object" groups="stock.group_stock_user" />
            </button>

        </field>
    </record>
</odoo>
